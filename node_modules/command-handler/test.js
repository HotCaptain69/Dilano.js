'use strict';

var test = require('tape');

var Command = require('.');

test('can enter and exit node repl terminal', function (t) {
  var nodeCommand = new Command('node', []);

  //nodeCommand.waitUntil('').then(function () {
    //return nodeCommand.input('console.log(1)');
  //})
  setTimeout(function () {
    nodeCommand.input('console.log(1)')
.then(function () {
    console.log(arguments);
    t.end();
  });
  }, 1000);
  
});
